"use client";

import { Button } from "@mui/material";

const NoticeList = () => {
  const asdf = async () => {};

  return (
    <div>
      <Button onClick={asdf}>asdf</Button>
    </div>
  );
};
export default NoticeList;
